---
layout: page
---

{%- assign cat = site.data.study_sections.categories | where: "id", page.category | first -%}
{%- assign tab = site.data.study_sections.tabs | where: "id", page.tab | first -%}

<style>
  /* No negative top margin: it pulled the crumb up into the descenders of the
     description above it, which clipped the text. */
  /* No negative top margin: it pulled the crumb up into the descenders of the
     description above and clipped it. The crumb also now carries the rule that
     separates the header from the body, since the meta strip below it is gone. */
  /* The crumb and the share row share one line, so the rule under them belongs
     to the line, not to the crumb. */
  .sn-head {
    display: flex; align-items: baseline; justify-content: space-between; gap: 1rem;
    margin: .75rem 0 1.4rem; padding-bottom: .7rem;
    border-bottom: 1px solid var(--global-divider-color);
  }
  .sn-head .sh-row { flex: 0 0 auto; margin: 0; }
  .sn-crumb { flex: 1 1 auto; min-width: 0; font-size: .76rem; line-height: 1.6; opacity: .62; }
  /* Too narrow for both: the crumb wraps and the buttons drop under it. */
  @media (max-width: 620px) {
    .sn-head { flex-direction: column; align-items: flex-start; gap: .55rem; }
  }
  .sn-crumb a { color: inherit; text-decoration: none; }
  .sn-crumb a:hover { color: var(--global-theme-color); text-decoration: underline; }
  .sn-foot { margin-top: 2.6rem; padding-top: 1.2rem; border-top: 1px solid var(--global-divider-color); }
  .sn-foot h3 { font-size: .74rem; font-weight: 800; letter-spacing: .08em; text-transform: uppercase;
                color: var(--global-text-color-light); opacity: .8; margin: 0 0 .5rem; }
  .sn-rel { list-style: none; padding: 0; margin: 0 0 1.5rem; }
  .sn-rel li { padding: .38rem 0; border-bottom: 1px solid var(--global-divider-color); font-size: .875rem; line-height: 1.45; }
  .sn-rel a { color: inherit; text-decoration: none; }
  .sn-rel a:hover { color: var(--global-theme-color); text-decoration: underline; }
  .sn-rel .sn-rel-sum { display: block; font-size: .78rem; opacity: .58; margin-top: .1rem; }
  .sn-rel .sn-soon { opacity: .5; }
  .sn-back { display: inline-block; margin-top: .4rem; font-size: .85rem; text-decoration: none; color: var(--global-theme-color); }
  .sn-back:hover { text-decoration: underline; }

  /* Long-form reading. The theme sets h2 at 2rem and h3 at 1.75rem, which is
     display sizing: on a note with thirty-odd headings an h3 arrives nearly as
     loud as the page title and the page reads as a stack of banners. These are
     section markers inside running prose, so they are sized to be found when
     scanning without interrupting when reading, and each binds to the text it
     introduces by taking much more space above than below. */
  .sn-body h2, .sn-body h3, .sn-body h4 {
    font-weight: 600; letter-spacing: -0.005em; line-height: 1.3;
    color: inherit; border: 0; padding: 0;
  }
  .sn-body h2 {
    font-size: 1.28rem; margin: 2.9rem 0 .75rem;
    padding-bottom: .4rem; border-bottom: 1px solid var(--global-divider-color);
  }
  .sn-body h3 { font-size: 1.05rem; margin: 2.1rem 0 .55rem; }
  .sn-body h4 {
    font-size: .93rem; margin: 1.5rem 0 .4rem; font-weight: 650;
    color: var(--global-text-color-light);
  }
  .sn-body > h2:first-child, .sn-body > h3:first-child { margin-top: 1.2rem; }

  /* No narrower measure than the title and the rule above it. A 68ch limit reads
     well in isolation but leaves the body visibly inset from the heading and the
     divider, which span the column, and the page looks misaligned. Line height
     carries the readability instead. */
  .sn-body p { line-height: 1.75; margin: 0 0 1.05rem; }
  .sn-body li { line-height: 1.7; margin-bottom: .3rem; }

  .sn-body table { font-size: .9rem; }
  .sn-body th { font-weight: 650; }
  .sn-body blockquote {
    border-left: 2px solid var(--global-divider-color);
    padding-left: .9rem; margin-left: 0; font-style: normal; opacity: .85;
  }
  .sn-body hr { margin: 2.6rem 0; border: 0; border-top: 1px solid var(--global-divider-color); }
  /* Footnote definitions are apparatus, not prose. */
  .sn-body .footnotes { font-size: .86rem; opacity: .75; }
  .sn-body .footnotes hr { margin-top: 3rem; }
</style>

{%- comment -%}
  Structured data. The gem owns <head>, so this sits in the body, which Google
  accepts for JSON-LD. Without it these pages carry no machine-readable statement
  of what they are, who wrote them, or when they changed.
{%- endcomment -%}
<script type="application/ld+json">
{
  "@context": "https://schema.org",
  "@type": "ScholarlyArticle",
  "headline": {{ page.title | strip_html | jsonify }},
  "description": {{ page.description | strip_html | jsonify }},
  "inLanguage": "en",
  "url": {{ page.url | absolute_url | jsonify }},
  "mainEntityOfPage": { "@type": "WebPage", "@id": {{ page.url | absolute_url | jsonify }} },
  "isPartOf": {
    "@type": "Collection",
    "name": "Study notes",
    "url": {{ "/study/" | absolute_url | jsonify }}
  },
  {%- if tab %}"articleSection": {{ tab.title | jsonify }},{% endif %}
  {%- if cat %}"about": {{ cat.title | jsonify }},{% endif %}
  {%- if page.updated %}"dateModified": {{ page.updated | jsonify }},{% endif %}
  "author": {
    "@type": "Person",
    "name": "Se-Hyeon Hwang",
    "alternateName": "황세현",
    "affiliation": { "@type": "Organization", "name": "Ajou University" },
    "url": {{ "/" | absolute_url | jsonify }}
  },
  "publisher": {
    "@type": "Person",
    "name": "Se-Hyeon Hwang",
    "url": {{ "/" | absolute_url | jsonify }}
  }
}
</script>

<div class="sn-head">
  <div class="sn-crumb">
    <a href="{{ '/study/' | relative_url }}">Study</a>
    {%- if tab %} / <a href="{{ '/study/' | relative_url }}#{{ tab.id }}">{{ tab.title }}</a>{% endif -%}
    {%- if cat %} / {{ cat.title }}{% endif -%}
    {%- if page.subgroup %} / {{ page.subgroup }}{% endif -%}
  </div>
  {% include share.liquid %}
</div>


<div class="sn-body">
{{ content }}
</div>

<div class="sn-foot">
  {%- assign siblings = site.study | where: "category", page.category | where_exp: "n", "n.url != page.url" | sort: "order" -%}
  {%- assign sib_written = siblings | where: "written", true -%}
  {%- if sib_written.size > 0 %}
  <h3>Related study notes</h3>
  <ul class="sn-rel">
    {%- for n in sib_written limit: 5 %}
    <li><a href="{{ n.url | relative_url }}">{{ n.title }}</a><span class="sn-rel-sum">{{ n.description }}</span></li>
    {%- endfor %}
  </ul>
  {%- endif %}

  {%- if page.papers and page.papers.size > 0 %}
  <h3>Related paper reviews</h3>
  <ul class="sn-rel">
    {%- for slug in page.papers %}
    {%- assign p = site.papers | where_exp: "r", "r.url contains slug" | first %}
    {%- if p %}
    <li><a href="{{ p.url | relative_url }}">{{ p.title }}</a><span class="sn-rel-sum">{{ p.venue }}</span></li>
    {%- endif %}
    {%- endfor %}
  </ul>
  {%- endif %}

  <a class="sn-back" href="{{ '/study/' | relative_url }}{% if tab %}#{{ tab.id }}{% endif %}">&larr; Back to {% if tab %}{{ tab.title }}{% else %}study{% endif %}</a>
</div>
